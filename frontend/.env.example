# Leave empty to call the API on the same origin (FastAPI serving the bundle).
# On Vercel set this to the Render backend URL, e.g.
# VITE_API_BASE_URL=https://triverify-api.onrender.com
VITE_API_BASE_URL=
